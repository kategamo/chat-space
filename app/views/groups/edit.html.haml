.chat-group-form
  %h1 チャットグループ編集
  =form_for @group do |g|
    #error_explanation
      %h2
        =@group.errors.count
        件のエラーがあります。
      %ul
      - @group.errors.full_messages.each do |msg|
        %li= msg
    .chat-group-form__field.clearfix
      .chat-group-form__field--left
        =g.label "グループ名",for: "chat_group_name", class: "chat-group-form__label"
      .chat-group-form__field--right
        =g.text_field :name, name:"group[name]", placeholder: "グループ名を入力してください", type: "text"
    .chat-group-form__field.clearfix
      .chat-group-form__field--left
        %label.chat-group-form__label{:for => "chat_group_チャットメンバーを追加"}
      .chat-group-form__field--right
        .chat-group-form__search.clearfix
          %input#user-search-field.chat-group-form__input{:placeholder => "追加したいユーザー名を入力してください", :type => "text"}/
        #user-search-result
    .chat-group-form__field.clearfix
      .chat-group-form__field--left
        =g.label "チャットメンバー", for: "chat_group_member", class: "label.chat-group-form__label"
    .chat-group-form__field.clearfix
      .chat-group-form__field--left
      .chat-group-form__field--right
        / グループ作成機能の追加時はここにcollection_check_boxesの記述を入れてください
        / .check_box
        / = g.collection_check_boxes(:user_ids, User.all, :id, :name) do |b|
        /   = b.label { b.check_box + b.text }
        / = g.submit data: {disable_with: "Save"}, name: "commit", value: "Save", class: "chat-group-form__action-btn"
